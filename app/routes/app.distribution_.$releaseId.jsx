import { useState } from "react";
import { useLoaderData, useNavigate, useRevalidator } from "react-router";
import { useAppBridge } from "@shopify/app-bridge-react";
import { boundary } from "@shopify/shopify-app-react-router/server";
import { authenticate } from "../shopify.server";
import db from "../db.server";
import { contributorDisplayName, creditRoleLabel, formatDate, typeLabel } from "../lib/releasecore";
import { authenticatedPost } from "../lib/authenticated-post";
import { distributionStatusLabel, distributionStatusTone, publishingTotal } from "../lib/workflow";
import { upcModeLabel } from "../lib/upc";
import { catalogModeLabel } from "../lib/catalog";

export const loader = async ({ request, params }) => {
  const { session } = await authenticate.admin(request);
  const [release, settings] = await Promise.all([
    db.release.findFirst({
      where: { id: params.releaseId, shop: session.shop },
      include: {
        artists: { include: { artist: true }, orderBy: { position: "asc" } },
        files: { orderBy: { createdAt: "desc" } },
        tracks: { orderBy: { position: "asc" }, include: { artists: { include: { artist: true }, orderBy: { position: "asc" } }, credits: { include: { contributor: true }, orderBy: { createdAt: "asc" } }, files: true } },
        events: { orderBy: { createdAt: "desc" }, take: 30 },
      },
    }),
    db.appSettings.findUnique({ where: { shop: session.shop } }),
  ]);
  if (!release) throw new Response("Release not found", { status: 404 });
  return { release, settings };
};

function StatusPill({ status }) { const tone=distributionStatusTone(status); const palette=tone==="good"?{background:"#eaf7ee",color:"#176c37"}:tone==="warn"?{background:"#fff4df",color:"#8a5700"}:tone==="info"?{background:"#eaf2ff",color:"#174ea6"}:{background:"#f1f1f1",color:"#4a4a4a"}; return <span style={{...styles.pill,...palette}}>{distributionStatusLabel(status)}</span>; }
function Readout({ label, value, mono=false }) { return <div style={styles.readout}><div style={styles.readoutLabel}>{label}</div><div style={{...styles.readoutValue,...(mono?styles.mono:{})}}>{value || "—"}</div></div>; }
function names(assignments, role) { return (assignments||[]).filter((a)=>a.role===role).map((a)=>a.artist?.name).filter(Boolean); }

export default function DistributionWorkspace() {
  const { release, settings } = useLoaderData(); const shopify=useAppBridge(); const navigate=useNavigate(); const revalidator=useRevalidator(); const [busy,setBusy]=useState(false); const [busyAction,setBusyAction]=useState(null); const [notice,setNotice]=useState(null); const [price,setPrice]=useState(String(settings?.defaultTrackPrice??1.29));
  const cover=release.files.find((f)=>f.kind==="COVER_ART"&&f.url); const createdCount=release.tracks.filter((t)=>t.shopifyProductId).length; const previewCount=release.tracks.filter((t)=>(t.files||[]).some((f)=>f.kind==="PREVIEW_MP3")).length; const displayDistributionStatus=release.distributionStatus==="NOT_QUEUED"&&release.status==="APPROVED"?"QUEUED":release.distributionStatus;
  const pendingMessage=(intent)=>({
    "generate-audio-previews":"Generating MP3 previews… This may take a moment.",
    "assign-upc":"Assigning UPC…",
    "assign-catalog":"Assigning catalog number…",
    "create-shopify-products":"Syncing Shopify products…",
    "save-manual-upc":"Saving UPC…",
    "save-manual-catalog":"Saving catalog number…",
    "update-distribution":"Updating distribution status…",
    "return-for-corrections":"Returning release for corrections…",
  }[intent]||"Processing…");
  const mutate=async(formData)=>{
    if(busy)return;
    const intent=String(formData.get("intent")||"");
    setBusy(true);
    setBusyAction(intent);
    setNotice({tone:"info",message:pendingMessage(intent)});
    try{
      const r=await authenticatedPost(shopify,`/api/distribution/${release.id}`,formData);
      const message=r.message||"Saved.";
      setNotice({tone:"good",message});
      shopify.toast.show(message);
      await revalidator.revalidate();
    }catch(e){
      setNotice({tone:"bad",message:e instanceof Error?e.message:"ReleaseCore could not update distribution."});
    }finally{
      setBusy(false);
      setBusyAction(null);
    }
  };
  const simple=(intent)=>{const f=new FormData();f.set("intent",intent);return mutate(f);};
  return <s-page heading={release.title}><style>{`@keyframes rcSpin{to{transform:rotate(360deg)}}`}</style>
    <s-button slot="secondary-actions" onClick={()=>navigate("/app/distribution")}>Distribution queue</s-button><s-button slot="secondary-actions" onClick={()=>navigate(`/app/release/${release.id}`)}>Release workspace</s-button>
    <s-section><div style={styles.hero}>{cover?<img src={cover.url} alt="Cover" style={styles.cover}/>:<div style={styles.coverEmpty}>No art</div>}<div style={{minWidth:0,flex:1}}><div style={styles.eyebrow}>Distribution workspace</div><div style={styles.titleLine}><div style={styles.title}>{release.title}</div><span style={styles.typePill}>{typeLabel(release.type)}</span><StatusPill status={displayDistributionStatus}/></div><div style={styles.heroMeta}>{release.artistName||"Artist not set"} · {release.tracks.length} {release.tracks.length===1?"track":"tracks"} · Release {formatDate(release.releaseDate)}</div></div></div></s-section>
    {notice?<div style={notice.tone==="good"?styles.noticeGood:notice.tone==="bad"?styles.noticeBad:styles.noticeInfo}>{notice.tone==="info"?<span style={styles.spinner} aria-hidden="true"/>:null}<span>{notice.message}</span></div>:null}

    <s-section heading="DSP delivery sheet"><div style={styles.intro}>A consolidated view for copying release information into Symphonic, DistroKid or another aggregator. Track-specific credits are listed below.</div><div style={styles.readoutGrid}>
      <Readout label="Release title" value={release.title}/><Readout label="Format" value={typeLabel(release.type)}/><Readout label="Primary artist" value={names(release.artists,"PRIMARY").join(" & ")||release.artistName}/><Readout label="Featured artists" value={names(release.artists,"FEATURED").join(", ")}/><Readout label="Primary genre" value={release.primaryGenre}/><Readout label="Release date" value={formatDate(release.releaseDate)}/><Readout label="UPC / GTIN-12" value={release.upc||"Pending"} mono/><Readout label="Catalog number" value={release.catalogNumber||"Pending"} mono/><Readout label="Label" value={settings?.defaultLabelName}/><Readout label="Copyright holder" value={settings?.defaultCopyrightHolder}/><Readout label="Aggregator reference" value={release.aggregatorReference}/>
    </div></s-section>

    <s-section heading="UPC"><div style={styles.intro}>{upcModeLabel(settings?.upcMode||"AGGREGATOR")}. UPCs identify the release; ISRCs continue to identify individual recordings.</div>
      {release.upc?<div style={styles.upcCard}><div><div style={styles.readoutLabel}>Assigned UPC</div><div style={styles.upc}>{release.upc}</div></div><span style={styles.goodPill}>Assigned</span></div>:settings?.upcMode==="GS1"?<div style={styles.actionBox}><div><strong>UPC not assigned yet</strong><div style={styles.muted}>ReleaseCore will allocate the next Item Reference from your configured GS1 prefix and calculate the GTIN-12 check digit.</div></div><button disabled={busy} onClick={()=>simple("assign-upc")} style={styles.primaryButton}>{busyAction==="assign-upc"?"Assigning UPC…":"Assign UPC"}</button></div>:<form style={styles.inlineForm} onSubmit={(e)=>{e.preventDefault();const f=new FormData(e.currentTarget);f.set("intent","save-manual-upc");mutate(f);}}><label style={{flex:1}}><span style={styles.inputLabel}>UPC provided by aggregator</span><input name="upc" inputMode="numeric" maxLength={12} placeholder="12-digit UPC / GTIN-12" style={styles.input}/></label><button disabled={busy} style={styles.primaryButton}>{busyAction==="save-manual-upc"?"Saving UPC…":"Save UPC"}</button></form>}
    </s-section>

    <s-section heading="Catalog number"><div style={styles.intro}>{catalogModeLabel(settings?.catalogMode||"AUTO")}. The catalog number is stored on every Shopify music product as a ReleaseCore metafield and is used to build the Shopify SKU.</div>
      {release.catalogNumber?<div style={styles.upcCard}><div><div style={styles.readoutLabel}>Assigned catalog number</div><div style={styles.upc}>{release.catalogNumber}</div></div><span style={styles.goodPill}>Assigned</span></div>:(settings?.catalogMode||"AUTO")==="AUTO"?<div style={styles.actionBox}><div><strong>Catalog number not assigned yet</strong><div style={styles.muted}>ReleaseCore will reserve the next number from your configured catalog sequence.</div></div><button disabled={busy} onClick={()=>simple("assign-catalog")} style={styles.primaryButton}>{busyAction==="assign-catalog"?"Assigning catalog…":"Assign catalog number"}</button></div>:<form style={styles.inlineForm} onSubmit={(e)=>{e.preventDefault();const f=new FormData(e.currentTarget);f.set("intent","save-manual-catalog");mutate(f);}}><label style={{flex:1}}><span style={styles.inputLabel}>Catalog number</span><input name="catalogNumber" maxLength={32} placeholder="Example: ERE260046" style={styles.input}/></label><button disabled={busy} style={styles.primaryButton}>{busyAction==="save-manual-catalog"?"Saving catalog…":"Save catalog number"}</button></form>}
    </s-section>

    <s-section heading="Tracks & delivery metadata"><div style={styles.trackList}>{release.tracks.map((track)=>{const primary=names(track.artists,"PRIMARY").join(" & ");const featured=names(track.artists,"FEATURED").join(", ");const previewReady=(track.files||[]).some((file)=>file.kind==="PREVIEW_MP3");return <div key={track.id} style={styles.trackCard}><div style={styles.trackHead}><div style={styles.trackNum}>{String(track.position).padStart(2,"0")}</div><div style={{minWidth:0,flex:1}}><div style={styles.trackTitle}>{track.title}{track.version?` (${track.version})`:""}</div><div style={styles.muted}>{primary||"Artist missing"}{featured?` feat. ${featured}`:""}</div></div><div style={{display:"flex",gap:6,flexWrap:"wrap",justifyContent:"flex-end"}}><span style={previewReady?styles.goodPill:styles.neutralPill}>{previewReady?"Preview ready":"No MP3 preview"}</span><span style={track.shopifyProductId?styles.goodPill:styles.neutralPill}>{track.shopifyProductId?"Shopify created":"Shopify pending"}</span></div></div><div style={styles.trackMeta}><Readout label="ISRC" value={track.isrc} mono/><Readout label="Language" value={track.language}/><Readout label="Explicit" value={track.explicit?"Yes":"No"}/><Readout label="Publishing" value={`${publishingTotal(track)}%`}/></div><div style={styles.creditBlock}><div style={styles.creditLabel}>Credits</div>{track.credits.length?<div style={styles.creditRows}>{track.credits.map((credit)=><div key={credit.id} style={styles.creditRow}><span><strong>{contributorDisplayName(credit.contributor)}</strong> · {creditRoleLabel(credit.role)}</span><span>{credit.ownershipPercent!=null?`${credit.ownershipPercent}%`:credit.contributor?.pro||credit.contributor?.ipi?[credit.contributor.pro,credit.contributor.ipi].filter(Boolean).join(" · "):""}</span></div>)}</div>:<div style={styles.muted}>No contributor credits.</div>}</div></div>;})}</div></s-section>

    <s-section heading="Audio previews"><div style={styles.intro}>Artists can listen to their original WAV master in the storefront portal before submission. When enabled in Settings, ReleaseCore can also create a browser-friendly MP3 preview, upload it to Shopify Files, and expose it on the Shopify product through <code>releasecore.audio_preview</code>.</div><div style={styles.shopifyBox}><div><div style={styles.bigStat}>{previewCount}/{release.tracks.length}</div><div style={styles.muted}>MP3 previews ready</div></div><div style={{flex:1,minWidth:220}}><strong>{settings?.generateShopifyAudioPreview?"Preview generation enabled":"Preview generation disabled"}</strong><div style={styles.muted}>{settings?.generateShopifyAudioPreview?`${settings.audioPreviewDurationSeconds===0?"Full-track":`${settings.audioPreviewDurationSeconds||60}-second`} · ${settings.audioPreviewBitrateKbps||192} kbps MP3` : "Enable it under Settings → Shopify audio previews."}</div></div><button type="button" disabled={busy||!settings?.generateShopifyAudioPreview} onClick={()=>simple("generate-audio-previews")} style={styles.primaryButton}>{busyAction==="generate-audio-previews"?"Generating MP3 previews…":previewCount?"Regenerate MP3 previews":"Generate MP3 previews"}</button></div><div style={styles.muted}>FFmpeg must be installed on the ReleaseCore server. Generated preview files are stored in Shopify Files and remain separate from the original WAV master.</div></s-section>

    <s-section heading="Shopify digital music products"><div style={styles.intro}>Creates or synchronizes one Shopify draft product per track. ReleaseCore fills the cover artwork, title, vendor, Digital Music Download classification, price, SKU and public music metadata. A Single's release UPC is written to the Shopify variant barcode. For EP/Album track products, the release UPC remains a release-level metafield rather than being duplicated as the barcode on every track.</div><div style={styles.shopifyBox}><div><div style={styles.bigStat}>{createdCount}/{release.tracks.length}</div><div style={styles.muted}>track products linked</div></div><label><span style={styles.inputLabel}>Track price</span><input type="number" min="0" step="0.01" value={price} onChange={(e)=>setPrice(e.target.value)} style={{...styles.input,width:120}}/></label><button disabled={busy} onClick={()=>{const f=new FormData();f.set("intent","create-shopify-products");f.set("price",price);mutate(f);}} style={styles.primaryButton}>{busyAction==="create-shopify-products"?"Syncing Shopify products…":createdCount?"Sync Shopify products":"Create Shopify products"}</button></div><div style={styles.muted}>SKU: {release.catalogNumber?release.type==="SINGLE"?release.catalogNumber:`${release.catalogNumber}-T01, -T02…`:"assigned from the catalog generator before product creation"}. Existing products are updated in place; deleted/stale Shopify links are detected and recreated.</div></s-section>

    <s-section heading="Distribution status"><form onSubmit={(e)=>{e.preventDefault();const f=new FormData(e.currentTarget);f.set("intent","update-distribution");mutate(f);}}><div style={styles.formGrid}><label><span style={styles.inputLabel}>Status</span><select name="distributionStatus" defaultValue={displayDistributionStatus} style={styles.input}><option value="QUEUED">Ready for distribution</option><option value="PROCESSING">Processing</option><option value="SUBMITTED_TO_STORES">Submitted to stores</option><option value="DELIVERED">Distribution complete</option></select></label><label><span style={styles.inputLabel}>Aggregator reference / submission ID</span><input name="aggregatorReference" defaultValue={release.aggregatorReference||""} style={styles.input}/></label></div><label style={{display:"block",marginTop:12}}><span style={styles.inputLabel}>Admin notes</span><textarea name="distributionNotes" defaultValue={release.distributionNotes||""} style={styles.textarea}/></label><div style={styles.footer}><button disabled={busy} style={styles.primaryButton}>{busyAction==="update-distribution"?"Updating status…":"Save distribution status"}</button></div></form>
      <div style={styles.correctionBox}><div><strong>Return for corrections</strong><div style={styles.muted}>Creates a change request, unlocks the release for correction, and keeps it visible in the Distribution Queue.</div></div><form onSubmit={(e)=>{e.preventDefault();const f=new FormData(e.currentTarget);f.set("intent","return-for-corrections");mutate(f);}} style={styles.correctionForm}><select name="trackId" style={styles.input}><option value="">Release-level correction</option>{release.tracks.map((t)=><option key={t.id} value={t.id}>Track {t.position} — {t.title}</option>)}</select><input name="message" required placeholder="What needs to be corrected?" style={styles.input}/><button disabled={busy} style={styles.dangerButton}>{busyAction==="return-for-corrections"?"Returning…":"Return"}</button></form></div>
    </s-section>

    <s-section slot="aside" heading="Queue status"><div style={styles.asideRow}><span>Distribution</span><strong>{distributionStatusLabel(displayDistributionStatus)}</strong></div><div style={styles.asideRow}><span>UPC</span><strong>{release.upc||"Pending"}</strong></div><div style={styles.asideRow}><span>Catalog</span><strong>{release.catalogNumber||"Pending"}</strong></div><div style={styles.asideRow}><span>Shopify</span><strong>{createdCount}/{release.tracks.length}</strong></div><div style={styles.asideRow}><span>Review</span><strong>{release.status}</strong></div><div style={styles.asideHelp}>Approval and distribution are separate state machines: review status records the approval decision, while distribution status tracks downstream delivery.</div></s-section>
  </s-page>;
}

const styles={hero:{display:"flex",gap:18,alignItems:"center"},cover:{width:96,height:96,objectFit:"cover",borderRadius:12,border:"1px solid #ddd"},coverEmpty:{width:96,height:96,borderRadius:12,background:"#f3f3f3",display:"grid",placeItems:"center",color:"#8c9196",fontSize:12},eyebrow:{fontSize:11,fontWeight:700,letterSpacing:".08em",textTransform:"uppercase",color:"#6d7175",marginBottom:7},titleLine:{display:"flex",alignItems:"center",gap:8,flexWrap:"wrap"},title:{fontSize:25,fontWeight:760,color:"#202223"},typePill:{display:"inline-flex",borderRadius:999,padding:"4px 8px",fontSize:11,fontWeight:700,background:"#f1f1f1"},pill:{display:"inline-flex",borderRadius:999,padding:"5px 9px",fontSize:11,fontWeight:700},heroMeta:{fontSize:12,color:"#6d7175",marginTop:7},intro:{fontSize:13,color:"#6d7175",lineHeight:1.5,marginBottom:14},readoutGrid:{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:10},readout:{border:"1px solid #e5e5e5",borderRadius:9,padding:10,background:"#fafafa",minWidth:0},readoutLabel:{fontSize:10,textTransform:"uppercase",letterSpacing:".06em",fontWeight:700,color:"#8c9196",marginBottom:4},readoutValue:{fontSize:13,color:"#303030",overflowWrap:"anywhere"},mono:{fontFamily:"ui-monospace,SFMono-Regular,Menlo,monospace",letterSpacing:".04em"},upcCard:{display:"flex",justifyContent:"space-between",alignItems:"center",gap:14,border:"1px solid #b8dfc2",background:"#f2fbf4",borderRadius:11,padding:14},upc:{fontFamily:"ui-monospace,SFMono-Regular,Menlo,monospace",fontSize:22,fontWeight:750,letterSpacing:".08em"},goodPill:{display:"inline-flex",borderRadius:999,padding:"4px 8px",fontSize:11,fontWeight:700,background:"#eaf7ee",color:"#176c37"},neutralPill:{display:"inline-flex",borderRadius:999,padding:"4px 8px",fontSize:11,fontWeight:700,background:"#f1f1f1",color:"#5c5f62"},actionBox:{display:"flex",justifyContent:"space-between",gap:18,alignItems:"center",border:"1px solid #e1e3e5",borderRadius:11,padding:14,background:"#fafafa"},inlineForm:{display:"flex",gap:10,alignItems:"end",flexWrap:"wrap"},inputLabel:{display:"block",fontSize:11,fontWeight:700,color:"#5c5f62",marginBottom:5},input:{height:40,border:"1px solid #8c9196",borderRadius:8,padding:"0 10px",font:"inherit",boxSizing:"border-box",width:"100%",background:"#fff"},textarea:{width:"100%",minHeight:90,border:"1px solid #8c9196",borderRadius:8,padding:10,font:"inherit",boxSizing:"border-box",resize:"vertical"},primaryButton:{appearance:"none",border:"1px solid #303030",borderRadius:8,background:"#303030",color:"#fff",minHeight:38,padding:"0 14px",font:"inherit",fontWeight:650,cursor:"pointer"},dangerButton:{appearance:"none",border:"1px solid #b42318",borderRadius:8,background:"#fff",color:"#b42318",minHeight:38,padding:"0 14px",font:"inherit",fontWeight:650,cursor:"pointer"},muted:{fontSize:12,color:"#6d7175",lineHeight:1.45},trackList:{display:"grid",gap:10},trackCard:{border:"1px solid #e1e3e5",borderRadius:12,padding:14,background:"#fff"},trackHead:{display:"flex",gap:12,alignItems:"center"},trackNum:{width:36,height:36,borderRadius:9,background:"#f4f4f4",display:"grid",placeItems:"center",fontWeight:750,fontSize:12,color:"#6d7175"},trackTitle:{fontSize:14,fontWeight:750,color:"#303030"},trackMeta:{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(150px,1fr))",gap:8,marginTop:12},creditBlock:{marginTop:12,paddingTop:12,borderTop:"1px solid #ededed"},creditLabel:{fontSize:11,fontWeight:750,color:"#5c5f62",marginBottom:7},creditRows:{display:"grid",gap:5},creditRow:{display:"flex",justifyContent:"space-between",gap:12,fontSize:12,color:"#45484b"},shopifyBox:{display:"flex",alignItems:"end",gap:18,flexWrap:"wrap",padding:14,border:"1px solid #e1e3e5",borderRadius:11,background:"#fafafa",marginBottom:10},bigStat:{fontSize:25,fontWeight:760},formGrid:{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(220px,1fr))",gap:12},footer:{display:"flex",justifyContent:"flex-end",marginTop:14},correctionBox:{borderTop:"1px solid #ededed",marginTop:18,paddingTop:18,display:"grid",gap:12},correctionForm:{display:"grid",gridTemplateColumns:"minmax(180px,240px) minmax(240px,1fr) auto",gap:8,alignItems:"end"},asideRow:{display:"flex",justifyContent:"space-between",gap:12,padding:"8px 0",borderBottom:"1px solid #ededed",fontSize:12},asideHelp:{fontSize:11,color:"#6d7175",lineHeight:1.45,marginTop:12},noticeInfo:{padding:12,border:"1px solid #b8c7df",background:"#eef4ff",borderRadius:9,color:"#174ea6",display:"flex",alignItems:"center",gap:9},spinner:{width:14,height:14,border:"2px solid rgba(23,78,166,.22)",borderTopColor:"#174ea6",borderRadius:"50%",display:"inline-block",flex:"0 0 auto",animation:"rcSpin .8s linear infinite"},noticeGood:{maxWidth:1000,margin:"0 auto 12px",borderRadius:8,background:"#eaf7ee",color:"#176c37",padding:"10px 13px",fontSize:13},noticeBad:{maxWidth:1000,margin:"0 auto 12px",borderRadius:8,background:"#fff1f0",color:"#8e1f0b",padding:"10px 13px",fontSize:13}};
export const headers=(headersArgs)=>boundary.headers(headersArgs);
